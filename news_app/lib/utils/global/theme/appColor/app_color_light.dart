import 'package:flutter/material.dart';
import 'package:news_app/utils/color_manager.dart';

class AppColorlight {
  static const appBarBackgroundColor = Color(colorManager.bottomnavbarcolor);
  static const scaffoldBackgroundColor = Color(colorManager.ScaffoldColor);
  static const bottomNavBarBackgroundColor =
      Color(colorManager.bottomnavbarbackgroundcolor);
  static const brimaryColor = Colors.white;
  static const titleTextStyle = Colors.black;
  static const dividerColor = Colors.black;
  static const iconColor = Colors.black;
}
